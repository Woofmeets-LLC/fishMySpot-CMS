'use strict';

/**
 * onboarding-source service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::onboarding-source.onboarding-source');
